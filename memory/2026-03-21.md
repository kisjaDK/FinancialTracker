## 2026-03-21

- `lib/finance/analysis.ts` and `components/finance/analysis-browser.tsx`: on `/analysis`, the `Actuals coverage gap` signal should only count months before the current month for the selected year (all months for past years, none for future years), and `Forecast concentration` should only rank external / managed-services (`AMS`) seats.
- `components/finance/analysis-browser.tsx`: the Analysis page should follow the tracker-summary display pattern for money values, with one shared `All financial values in DKK` note, no repeated `DKK` prefixes inside cards, and negative amounts shown in accounting style with red text.
- `tests/analysis.test.ts`: keep the budget outlook facts test pinned with an explicit `asOfDate` so current-month coverage behavior stays deterministic.
- `lib/finance/queries.ts`: pasted invoice parsing must not strip every `.` from `Net Total`; invoice emails may use either `41185.22` or `41.185,22`, so amount parsing now detects decimal vs thousands separators before normalizing.
- `tests/finance.test.ts`: keep regression coverage for pasted invoice amount parsing across both English-style and European-style number formats.
- `components/finance/actuals-browser.tsx`: the external actuals ingestion mode can be restored from the `externalMode` query param, and the pasted-invoice save flow should redirect back to `/actuals?view=external&externalMode=paste...` so users can add another invoice without reselecting the form.
- `components/finance/actuals-browser.tsx`: external actual CSV/manual/paste saves should avoid `window.location.href`; use client-side navigation with `scroll: false` plus `router.refresh()` so the page data updates without jumping back to the top.
- `components/finance/actuals-browser.tsx`: after a pasted invoice is saved successfully, clear `pastedInvoiceContent` before refreshing the external actuals view so the `Paste Invoice` form is ready for the next entry.
- `components/finance/actuals-browser.tsx`, `app/api/external-actuals/manual/route.ts`, and `lib/finance/queries.ts`: the manual external actual form now uses the maintained vendor list for supplier selection, supports searching external seats by spend plan / seat ID / name, and can split a manual invoice across explicitly selected seats by allocation instead of relying only on spend-plan matching.
- `lib/finance/queries.ts`: explicit manual-seat matching must preserve selected seat order when splitting amounts, validate that selected seats are active external seats within the viewer scope, and fall back to each seat's own `spendPlanId` when the form-level spend plan is blank.
- `components/finance/actuals-browser.tsx`: the manual form now uses inline async typeaheads for spend plan, seat ID, and name; selecting a suggestion immediately adds that seat to the chosen-seat list, so the old separate picker/results section should stay removed.
- `components/finance/actuals-browser.tsx`: spend plan and name typeaheads should stay usable for repeated multi-pick seat selection; selecting a suggestion from those fields keeps the typeahead active and marks already chosen seats, while seat ID remains a one-shot direct picker.
- `components/finance/actuals-browser.tsx` and `app/api/external-actuals/manual-preview/route.ts`: supplier selection now uses a searchable typeahead over maintained vendors, and non-DKK manual amounts should show a muted converted DKK helper line fetched from a server-side preview endpoint that uses the same year/month FX lookup logic as saved actuals.
- Validation for this change: `npm test -- --test-name-pattern="parsePastedInvoiceAmount"` and `npm run build`.
- `lib/finance/derive.ts`: cloud seats must be treated as their own forecast bucket, not as permanent seats; they should contribute to `cloudCostForecast` only through remaining forecast months, and must not inflate `permTarget` or `permForecast`.
- `components/finance/workspace.tsx`: the tracker Selected Pillar card should render `PERM Target` as a raw FTE-style number, not via `formatPercent`, or values like `52` will appear as `5,200%`.
- `app/forecasts/page.tsx`, `lib/finance/queries.ts`, and `components/finance/forecasts-browser.tsx`: tracker-to-forecast drill-through now relies on `projectCode` plus a `forecastBucket` query param (`perm`, `ext`, `cloud`) so a selected pillar can open the correct underlying seat subset on `/forecasts`.
- Validation for this change: `npm run build`.
- `lib/finance/queries.ts`: saving cloud actuals must prefer an existing `resourceType = "cloud"` seat in the same budget area/scope instead of always creating or reusing the synthetic `manual-cloud:*` seat; the synthetic seat is only a fallback when no real cloud seat exists.
- `lib/finance/queries.ts`: when a real cloud seat and a synthetic `manual-cloud:*` duplicate both exist, synthetic month actuals can be migrated onto the real seat if the target months are empty, after which the synthetic seat should be deactivated to remove the duplicate from tracker views.
- Validation for this change: `npm run build`.
- `lib/finance/types.ts`, `lib/finance/queries.ts`, and `components/finance/actuals-browser.tsx`: the Actuals Cloud tab summary should expose the preferred underlying cloud seat identity so the `Forecasted Value` card can tell users exactly which seat the month forecast and saved actual map to.
- Validation for this change: `npm run build`.
- `lib/finance/types.ts`, `lib/finance/queries.ts`, and `components/finance/actuals-browser.tsx`: cloud deviation UI must use a month-level comparison forecast series based on `usedForecastAmount` when actuals already exist; using the remaining forecast series makes historical months show `DKK 0` forecast and incorrect deviation.
- Validation for this change: `npm run build`.
- `lib/finance/queries.ts`: `applyTrackerSeatMonthUpdate`, `updateTrackerSeat`, and bulk month updates must snapshot `usedForecastAmount` whenever a month is being switched to `forecastIncluded = false`, even if an actual amount is being written in the same request; otherwise cloud/internal actual deviation views have no original forecast baseline to compare against.
- `lib/finance/queries.ts`: for older seat months where `usedForecastAmount` is still null, cloud comparison forecast should be reconstructed at read time by temporarily re-including that month's forecast rather than showing `DKK 0`.
- Validation for this change: `npm run build`.
- `components/finance/workspace.tsx`: the Selected Pillar forecast cards on `/tracker` should filter the in-page seat tracker by forecast bucket (`perm`, `ext`, `cloud`) instead of routing to `/forecasts`; keep the active bucket in the tracker query string so the seat list, apply/reset actions, and selected-card state stay in sync.
- Validation for this change: `npm run build`.

- `components/finance/workspace.tsx`: in the tracker seat table, the `Year End Spent` deviation under `vs full year` should treat negative values as favorable (green) and positive values as unfavorable (rose), because lower year-end cost than forecast is good.

- `app/tracker/page.tsx` and `components/finance/workspace.tsx`: the tracker seat filter panel now has a `Show cancelled seats` switch, defaulting to hidden unless `showCancelledSeats=true` is present in the query string; cancelled uses the same normalized labels as the tracker hide logic (`Cancelled` and `Cancelled- account still active in AD`).

- `components/finance/workspace.tsx`: the tracker seat table now uses an inner scroll container with `height: max(20rem, calc(100dvh - 24rem))` so the seat list stays viewport-bounded, and its header cells are sticky with a solid background so column labels remain visible while scrolling.

- `components/finance/workspace.tsx`: the Selected Pillar card now includes a second aligned three-card row under the forecast cards for `PERM Budget - Spent`, `EXT Budget - Spent`, and `Cloud Budget - Spent`, calculated from the selected pillar seat rows (`yearlyCostInternal/yearlyCostExternal` minus spent, and cloud target minus cloud spent).

- `components/finance/workspace.tsx`: Selected Pillar bucket summary cards must use the derived seat bucket flags (`extFte` / `permFte`) instead of a raw `resourceType === "external"` check, otherwise managed-service or vendor-backed external seats fall into PERM and the `EXT Budget - Spent` card can incorrectly show zero.

- `lib/finance/queries.ts`, `lib/finance/types.ts`, and `components/finance/workspace.tsx`: Selected Pillar `PERM/EXT Budget - Spent` must use real budget movement category totals, not `yearlyCostInternal/yearlyCostExternal`. For the live `Customer, Channel, Colleague & Brand / L68210001` example, the valid split is `perm 49,525,000 + ext 4,700,000 + cloud 7,376,000 = 61,601,000`, so cloud categories map to cloud, explicit `ext/external` categories map to ext, and other non-cloud budget movements default to perm.

- `prisma/schema.prisma`, `lib/finance/queries.ts`, `components/finance/admin-browser.tsx`, and `app/api/admin/budget-movement-category-mappings/route.ts`: budget movement category-to-bucket mapping is now admin-managed through a dedicated `BudgetMovementCategoryMapping` table with buckets `PERM`, `EXT`, `CLOUD`, `AMS`, and `LICENSES`. The Admin `Other` tab lists discovered categories and lets admins save or reset explicit mappings, while tracker budget derivation uses the saved mapping when present and falls back to inference (`cloud`, `ext/external`, `ams`, `license/licence`, else `perm`).

- `lib/finance/derive.ts`, `lib/finance/queries.ts`, `lib/finance/types.ts`, and `components/finance/workspace.tsx`: AMS is now a first-class tracker bucket. Seats with `resourceType` containing `managed service(s)` or `ams` are split out of `EXT`, get their own `amsForecast`/`amsFte`, and the Selected Pillar card now renders forecast and budget-minus-spent rows as four columns: `PERM`, `EXT`, `AMS`, and `Cloud`.

- `components/finance/workspace.tsx`: the Seat Tracker summary strip now shows `Showing X of Y FTE` based on seat `allocation`, comparing currently listed rows against all rows in the selected pillar, while excluding cloud and license/licence resource types from both totals.

- `components/finance/workspace.tsx`: the Seat Tracker table now uses `table-fixed` with wrapped text on the Seat/Resource/Type columns to avoid desktop horizontal scrolling from long names, and the table header row itself is sticky inside the scroll container so column headings remain pinned while scrolling the seat list.

- `components/finance/workspace.tsx`: sticky behavior for the Seat Tracker header must live on each `TableHead` cell (`sticky top-0`) rather than the `TableHeader` wrapper; the wrapper-level sticky was not reliably pinning the headings during seat-list scrolling.

- `components/ui/table.tsx` now accepts an optional `containerClassName` so tables can override the default wrapper overflow. The Seat Tracker table uses `containerClassName="overflow-visible"` because the default `overflow-x-auto` wrapper prevented sticky header cells from pinning to the actual vertical scroll container.

- `components/finance/actuals-browser.tsx`, `app/api/external-actual-entries/[entryId]/route.ts`, and `lib/finance/queries.ts`: editing an external actual on `/actuals` can now change the posting month. The update path persists `monthIndex/monthLabel` and re-syncs both the old and new tracker seat month when the entry moves months, so the source month is cleared or backfilled from any remaining latest external entry.
- `components/finance/actuals-browser.tsx` and `lib/finance/queries.ts`: imported external actual rows should update immediately from the PATCH response instead of relying only on `router.refresh()`, and edited month labels must keep the year-aware format via `monthLabel(year, monthIndex)` so the table stays consistent with imported values like `Jan-26`/`Feb-26`.
- `components/finance/actuals-browser.tsx`, `app/api/tracker/bulk-forecast-actuals/route.ts`, and `lib/finance/queries.ts`: internal actuals now support an undo flow for bulk `Convert Forecast to Actual`, scoped to the currently selected sub-domain/project and a chosen month. Undo preview/apply should only target rows that still match the untouched bulk-copy pattern (`forecastIncluded=false`, `usedForecastAmount` present, DKK/1x FX, actual still equal to the copied forecast) so generic manual internal actuals are not rolled back.
- `components/finance/actuals-browser.tsx`: the internal actuals seat table and manual seat selector must use an internal-only subset of `seats` rather than the full scope list. Filtering by `permFte > 0` and `extFte <= 0` keeps external rows out of the Internals tab, and the seat ID / forecast cells can safely deep-link to `/people-roster?seatId=...` and `/forecasts?seatId=...&selectedSeatId=...`.
- `components/finance/actuals-browser.tsx`: for the Internals tab, the seat-table drill-through links were better moved out of the table and into the `Seat Monthly Detail` header. The table now stays read-only, while the selected-seat summary links to People Roster and exposes a small `Open Forecast` CTA for the active seat.
- `components/finance/workspace.tsx`: the Budget Summary table on `/tracker` should not use `table-fixed` without explicit widths; it collapses the Pillar/Project cells into the numeric columns. Giving each column a real width, top-aligning rows, and keeping numeric cells right-aligned with nowrap fixes the layout while the shared table container still provides horizontal scroll when needed.
- `components/finance/workspace.tsx`: the separate `Project` column in the Budget Summary table was not earning its width. Moving project code into the Pillar cell as a quieter third line simplifies the layout and reduces horizontal pressure; remember to lower the empty-state `colSpan` accordingly.
- `components/finance/workspace.tsx`: the Budget Summary table now carries a single `All values in DKK` note near the title, and its numeric cells strip the repeated `DKK` prefix with a table-local formatter so this cleanup does not affect currency formatting elsewhere in the app.
- `components/finance/workspace.tsx`: Budget Summary negatives now use accounting-style display for readability: format them as `(amount)` and apply a rose text color on the affected numeric cells, while leaving positive values unchanged.
- `components/finance/workspace.tsx`: Budget Summary header labels can be wrapped intentionally with stacked spans (`Spent To` / `Date`, `End Of Year` / `Balance`, etc.) so the numeric columns can be narrowed further without awkward browser-driven wrapping.
