## 2026-03-21

- `lib/finance/queries.ts`: pasted invoice parsing must not strip every `.` from `Net Total`; invoice emails may use either `41185.22` or `41.185,22`, so amount parsing now detects decimal vs thousands separators before normalizing.
- `tests/finance.test.ts`: keep regression coverage for pasted invoice amount parsing across both English-style and European-style number formats.
- `components/finance/actuals-browser.tsx`: the external actuals ingestion mode can be restored from the `externalMode` query param, and the pasted-invoice save flow should redirect back to `/actuals?view=external&externalMode=paste...` so users can add another invoice without reselecting the form.
- `components/finance/actuals-browser.tsx`: external actual CSV/manual/paste saves should avoid `window.location.href`; use client-side navigation with `scroll: false` plus `router.refresh()` so the page data updates without jumping back to the top.
- `components/finance/actuals-browser.tsx`: after a pasted invoice is saved successfully, clear `pastedInvoiceContent` before refreshing the external actuals view so the `Paste Invoice` form is ready for the next entry.
- `components/finance/actuals-browser.tsx`, `app/api/external-actuals/manual/route.ts`, and `lib/finance/queries.ts`: the manual external actual form now uses the maintained vendor list for supplier selection, supports searching external seats by spend plan / seat ID / name, and can split a manual invoice across explicitly selected seats by allocation instead of relying only on spend-plan matching.
- `lib/finance/queries.ts`: explicit manual-seat matching must preserve selected seat order when splitting amounts, validate that selected seats are active external seats within the viewer scope, and fall back to each seat's own `spendPlanId` when the form-level spend plan is blank.
- `components/finance/actuals-browser.tsx`: the manual form now uses inline async typeaheads for spend plan, seat ID, and name; selecting a suggestion immediately adds that seat to the chosen-seat list, so the old separate picker/results section should stay removed.
- `components/finance/actuals-browser.tsx`: spend plan and name typeaheads should stay usable for repeated multi-pick seat selection; selecting a suggestion from those fields keeps the typeahead active and marks already chosen seats, while seat ID remains a one-shot direct picker.
- `components/finance/actuals-browser.tsx` and `app/api/external-actuals/manual-preview/route.ts`: supplier selection now uses a searchable typeahead over maintained vendors, and non-DKK manual amounts should show a muted converted DKK helper line fetched from a server-side preview endpoint that uses the same year/month FX lookup logic as saved actuals.
- Validation for this change: `npm test -- --test-name-pattern="parsePastedInvoiceAmount"` and `npm run build`.
