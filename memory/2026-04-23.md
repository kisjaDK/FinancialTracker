## 2026-04-23

- People roster CSV imports now use a dedicated `parseRosterCsvDate()` helper so `Start Date` and `End Date` are interpreted as U.S. `MM/DD/YYYY` values without changing the broader `parseDate()` behavior used by other import paths.
- Historical-roster filtering must use the same roster-specific date parser, otherwise ambiguous slash-separated dates can be classified against the wrong month/day ordering before import.
- `npm run build` currently fails in this workspace for an unrelated reason: `next/font/google` tries to fetch `Geist` and `Geist Mono` from Google Fonts, which is blocked in the sandbox/offline environment.
