## 2026-03-16

- `app/api/tracker/export/route.ts`: tracker budget-summary exports are domain-scoped, require `year` and `domain`, and must reject users who only have sub-domain scopes; use `hasFullDomainAccess` before exporting.
- `components/finance/workspace.tsx`: the tracker Budget Summary export button should stay disabled unless a mapped domain is selected and the viewer has full-domain access; show the reason inline so restricted users understand why export is unavailable.
- `lib/finance/queries.ts`: domain exports should reuse tracker summary grouping logic (`buildBudgetAreaSummaryFromSnapshot` + summary keys) so exported seats match the selected domain exactly, even when the domain comes from mapped budget-area hierarchy rather than the seat row alone.
- `lib/finance/csv.ts`: `serializeCsv` now accepts a custom delimiter; use `;` for tracker domain exports and keep the default comma behavior for existing CSV flows.
