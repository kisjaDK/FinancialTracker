## 2026-03-16

- `app/api/tracker/export/route.ts`: tracker budget-summary exports are domain-scoped, require `year` and `domain`, and must reject users who only have sub-domain scopes; use `hasFullDomainAccess` before exporting.
- `components/finance/workspace.tsx`: the tracker Budget Summary export button should stay disabled unless a mapped domain is selected and the viewer has full-domain access; show the reason inline so restricted users understand why export is unavailable.
- `components/finance/workspace.tsx`: in the Budget Summary header, keep `Year` and `Domain` grouped on the left, and only render the export hint/button when the current domain is actually exportable; use a flex spacer so the export controls sit flush right.
- `lib/finance/queries.ts`: domain exports should reuse tracker summary grouping logic (`buildBudgetAreaSummaryFromSnapshot` + summary keys) so exported seats match the selected domain exactly, even when the domain comes from mapped budget-area hierarchy rather than the seat row alone.
- `lib/finance/csv.ts`: `serializeCsv` now accepts a custom delimiter; use `;` for tracker domain exports and keep the default comma behavior for existing CSV flows.
- `components/finance/workspace.tsx`: Biome prefers canonical Tailwind classes like `w-28`, `max-w-48`, and `wrap-break-word`; when replacing `findIndex` with `indexOf` against `MONTH_NAMES`, cast the lookup value to `(typeof MONTH_NAMES)[number]` so Next.js TypeScript still accepts the tuple lookup.
- Custom skills can live in `/Users/kimstuhrjakobsen/.codex/skills/`; initialize new ones with the system `skill-creator` `init_skill.py`, keep `SKILL.md` lean, move detailed heuristics into `references/`, and validate with `quick_validate.py`.
- `senior-ux-ui-designer` was created as a critique-and-direction skill, not an implementation skill; pair it with `frontend-design` or `nextjs-shadcn-frontend` when a task also needs code changes.
- Staffing vs. People Roster discrepancies can come from imported roster data rather than staffing aggregation: for 2026 `Data & Analytics` / `Crafting, Supply, Merchandising & Finance`, seat `300262` is imported from `FullOPR.csv` as `status = Open` while still carrying `resourceName = Tiffany Qiao`; `deriveTrackerSeatsForYear` copies that raw combination into `trackerSeat`.
- The staffing page’s May 2026 `open PERM = 4` for `Crafting, Supply, Merchandising & Finance` is backed by seats `300262`, `300268`, `300299`, and `300373`; if users question the count, inspect `rosterPerson` and `trackerSeat` for those seat IDs before changing staffing logic.
