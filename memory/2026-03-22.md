## 2026-03-22

- `lib/finance/analysis.ts`, `components/finance/analysis-browser.tsx`, and `tests/analysis.test.ts`: on `/analysis`, `Forecast Spend To End Of Year` should show `summary.totalForecast`, not the uncovered-actuals amount from `Coverage gaps`.
- `lib/finance/analysis.ts` and `components/finance/analysis-browser.tsx`: the Analysis deterministic facts panel now carries the same bucket cards as tracker for `PERM/EXT/AMS/Cloud Forecast` and `Budget - Spent`, using tracker-detail seat totals to calculate bucket-specific spent amounts rather than approximating from overall summary values.
- `lib/finance/analysis.ts` and `components/finance/analysis-browser.tsx`: `Forecast concentration` on `/analysis` is defined as concentration of external + AMS forecast spend across the top seats by forecast amount; it is not an uncovered-actuals metric, and the signal/card should show both the concentrated amount and the share of the external+AMS forecast pool.
- `lib/finance/analysis.ts`: analysis bucket cards and concentration cannot rely on `resourceType` string matching alone; they need the same seat-bucket classification as `/tracker`, using `amsFte`, `extFte`, and `permFte` from tracker detail so external spend is still classified correctly when the resource type label itself is not literally `external`.
- `lib/finance/analysis.ts`: the analysis precomputed signals now include bucket-performance signals for `PERM`, `EXT`, `AMS`, and `Cloud`, combining current-year spend-versus-paced-budget context with projected year-end forecast-versus-budget so the AI can comment on how each category is performing now and at year-end.
- `components/finance/analysis-browser.tsx`: negative values in Analysis precomputed signals should not always be red; when a signal is marked `favorable` (for example underspend versus budget in bucket-performance signals), the amount should render green to match the signal direction.
- Validation for this change: `npm test -- --test-name-pattern="buildBudgetOutlookFactsFromData computes forecast and coverage signals"` and `npm run build`.
- `docs/funding-linking-prd.md`: funding-linking product documentation should use `funding` as the app-level linkage key between seats and budget movements, explicitly treat `RosterPerson.fundingType` as only an import source term, and preserve append-only budget movement history with grouped net summaries by funding.
- `docs/funding-linking-prd.md`: the funding-linking PRD now also defines a tracker-scoped funding follow-up page at `/tracker/funding-follow-up` plus advisory funding-capacity feedback in the seat editor dialog, both reusing existing effective-seat, actuals, and forecast derivation logic.
- `prisma/schema.prisma`, `lib/finance/queries.ts`, `lib/finance/imports.ts`, `components/finance/seat-editor-dialog.tsx`, `components/finance/people-roster-browser.tsx`, and `components/finance/budget-movements-browser.tsx`: funding-linking implementation reuses the existing `funding` fields on seats and budget areas, adds a new receiving-side `BudgetMovement.funding`, and introduces `SeatReferenceValueType.FUNDING` so year-specific funding values can be maintained and auto-seeded from roster imports, budget-movement imports, manual movement entry, and seat edits.
- `components/finance/budget-movements-browser.tsx` and `app/api/budget-movements/export/route.ts`: the budget movements page now supports funding-aware manual entry, grouped net summaries by funding, and CSV export of row-level movement history including funding; manual movement year switching should load funding values from `/api/seat-reference-values?year=...&type=FUNDING`.
- `components/finance/budget-movements-browser.tsx`: the Movements table now supports client-side ascending/descending sorting by clicking column headers for date, source, category, funding, giving, receiving, hierarchy, notes, finance view, and amount given; default sort remains newest date first.
- `app/tracker/funding-follow-up/page.tsx`, `components/finance/funding-follow-up-browser.tsx`, and `lib/finance/queries.ts`: tracker now has a funding follow-up subpage that groups allocated movement funding against seat actuals and year-end forecast by effective seat funding, with same-page seat drilldown and scope-aware year filtering.
- `components/finance/workspace.tsx`, `app/tracker/funding-follow-up/page.tsx`, and `lib/finance/queries.ts`: the funding follow-up page now also supports domain and sub-domain scoping, and tracker launches it from the Selected Pillar card so the selected pillar scope pre-populates the follow-up filters.
- `lib/finance/queries.ts`, `components/finance/seat-editor-dialog.tsx`, and `components/finance/workspace.tsx`: funding availability and funding follow-up scoping now also include project code, so seat-dialog funding remaining/exceeded amounts reflect the seat’s related project rather than the wider funding pool.
- `components/finance/funding-follow-up-browser.tsx`: the Funding Overview table hides rows where allocated, used, and projected are all zero, notes that amounts are in DKK at the card level, omits repeated `DKK` prefixes in cells, and renders negative amounts with accounting parentheses.
- `components/finance/workspace.tsx`: tracker page card groups now state DKK once at the section/card level and render card-style monetary values without repeated `DKK` prefixes, reusing the existing no-prefix budget-summary formatter for consistent display.
- `components/finance/workspace.tsx`: the Seat Tracker section and monthly detail dialog now follow the same DKK display rule, with a single DKK note at the section/dialog level and no repeated `DKK` prefixes in listed totals or table amounts.
- `components/finance/seat-editor-dialog.tsx` and `app/api/tracker/funding-follow-up/preview/route.ts`: the seat editor now calls a server-side funding preview endpoint and shows advisory remaining/exceeded funding feedback using the same tracker derivation logic; preview stays neutral until funding, dates, allocation, and the required rate/internal-cost inputs are present.
- Validation for this change: `npm run db:generate` and `npm run build`.
