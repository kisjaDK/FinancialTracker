## 2026-03-22

- `lib/finance/analysis.ts`, `components/finance/analysis-browser.tsx`, and `tests/analysis.test.ts`: on `/analysis`, `Forecast Spend To End Of Year` should show `summary.totalForecast`, not the uncovered-actuals amount from `Coverage gaps`.
- `lib/finance/analysis.ts` and `components/finance/analysis-browser.tsx`: the Analysis deterministic facts panel now carries the same bucket cards as tracker for `PERM/EXT/AMS/Cloud Forecast` and `Budget - Spent`, using tracker-detail seat totals to calculate bucket-specific spent amounts rather than approximating from overall summary values.
- `lib/finance/analysis.ts` and `components/finance/analysis-browser.tsx`: `Forecast concentration` on `/analysis` is defined as concentration of external + AMS forecast spend across the top seats by forecast amount; it is not an uncovered-actuals metric, and the signal/card should show both the concentrated amount and the share of the external+AMS forecast pool.
- `lib/finance/analysis.ts`: analysis bucket cards and concentration cannot rely on `resourceType` string matching alone; they need the same seat-bucket classification as `/tracker`, using `amsFte`, `extFte`, and `permFte` from tracker detail so external spend is still classified correctly when the resource type label itself is not literally `external`.
- `lib/finance/analysis.ts`: the analysis precomputed signals now include bucket-performance signals for `PERM`, `EXT`, `AMS`, and `Cloud`, combining current-year spend-versus-paced-budget context with projected year-end forecast-versus-budget so the AI can comment on how each category is performing now and at year-end.
- `components/finance/analysis-browser.tsx`: negative values in Analysis precomputed signals should not always be red; when a signal is marked `favorable` (for example underspend versus budget in bucket-performance signals), the amount should render green to match the signal direction.
- Validation for this change: `npm test -- --test-name-pattern="buildBudgetOutlookFactsFromData computes forecast and coverage signals"` and `npm run build`.
