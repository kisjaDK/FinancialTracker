## 2026-03-15

- `components/finance/actuals-browser.tsx`: the "Review forecast copy to actuals" dialog now expects both adjusted `amount` and unadjusted `baseAmount` so it can show the original forecast in muted text when a forecast override changed the month value.
- `components/finance/actuals-browser.tsx`: the bulk forecast copy dialog header now places the processed month in the top-right of the title row instead of repeating it in the description.
- `components/finance/actuals-browser.tsx`: the processed month label in the bulk forecast copy dialog uses a right-offset badge style (`mr-10`) to avoid the close button and should stay visually strong enough to read as part of the header.
- `components/finance/actuals-browser.tsx`: long status values in the bulk forecast copy dialog should be rendered inside a truncated badge with a hover tooltip so full status text remains accessible; the current compact width is `max-w-24`.
- `components/finance/actuals-browser.tsx`: adjusted forecast values in the bulk forecast copy dialog should reuse the `PenLine` icon used in forecasts so users can immediately distinguish changed forecast amounts.
- `components/finance/actuals-browser.tsx`: in the pasted invoice review dialog, the daily-rate validation is DKK-based and the UI should show the stored daily rate as DKK, not in the invoice currency.
- `components/finance/actuals-browser.tsx`: the pasted invoice review seat table is easier to scan when the DKK amount is primary with non-DKK original amount muted beneath it, and when billed days are primary with the daily rate muted beneath; keep allocation abbreviated as `Alloc` with a narrow column.
- `components/finance/user-admin-browser.tsx`: User Admin should not offer edit or delete actions for `SUPER_ADMIN` users; show the restriction directly in the UI.
- `lib/authz.ts`: `canManageRole` must return `false` whenever the current target role is `SUPER_ADMIN`, which prevents role changes and deletions of super-admin accounts through shared admin flows.
- `lib/finance/queries.ts`: `getInternalForecastCopyCandidates` should derive both normal metrics and `ignoreForecastOverrides: true` base metrics when a UI needs to compare adjusted forecast values against the underlying unadjusted forecast.
- `lib/finance/queries.ts`: pasted invoice seat validation should compute `daysEquivalent` from the seat's DKK-converted amount share divided by `dailyRate`; using the original invoice currency overstates or understates non-DKK invoices.
- `components/finance/admin-browser.tsx`: the admin FX card should show full rate history per currency, not just the latest row, while keeping each historical rate editable from the table.
- `lib/finance/queries.ts`: pasted invoice FX conversion should use the latest exchange rate on or before the selected posting month's last day; the preview should also show which FX effective date was used.
